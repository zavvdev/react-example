export const DOMAIN = "rsf";

export const ACTION_TYPE_PATTERNS = {
  request: "rsf@request",
  success: "rsf@success",
  failure: "rsf@failure",
};

export const DEFAULT_REQUEST_OPTIONS = {
  useCache: true,
};
