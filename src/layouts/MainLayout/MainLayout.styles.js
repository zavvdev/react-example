import { createUseStyles } from "react-jss";

const useMainLayoutStyles = createUseStyles({
  root: {
    padding: "0 5% 200px 5%",
  },

  content: {
    paddingTop: "42px",
  },
});

export { useMainLayoutStyles };
