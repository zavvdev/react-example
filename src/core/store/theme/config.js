export const THEME_DOMAIN = "theme";

export const THEME_ACTION_TYPES = {
  toggleDarkMode: `${THEME_DOMAIN}/toggleDarkMode`,
};

export const IS_DARK_MODE_LOCAL_STORAGE_KEY = `@${THEME_DOMAIN}_isDarkMode`;
